#!/bin/sh

if [ $# -ne 0 ]; then
    exit 1;
fi

LN_HOME_DIR="$( dirname $( readlink -f "${0}" ) )/home"

for file in $( find "${LN_HOME_DIR}" -maxdepth 1 -mindepth 1 ); do
    ln -s -i -t "${HOME}" "${file}"
done
